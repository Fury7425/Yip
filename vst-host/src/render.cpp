// render.cpp — offline file -> file rendering. M5.
//
// Intentionally empty until the VST3 SDK is wired in. Listed in
// CMakeLists.txt so the file exists from M1 onward (matches the directory
// layout in CLAUDE.md).

namespace yip::vst {
    // M5 entry: render(const std::filesystem::path& in,
    //                  const std::filesystem::path& out,
    //                  std::span<PluginInstance*> chain,
    //                  std::function<void(float)> on_progress);
}  // namespace yip::vst
